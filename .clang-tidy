---
Checks: >
  -*,
  bugprone-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-exception-escape,
  clang-analyzer-*,
  performance-*,
  -performance-enum-size,
  -performance-avoid-endl,
  modernize-use-override,
  modernize-use-nullptr

WarningsAsErrors: ''

# Lint project code under lib/* except vendored CLI11 headers (lib/cli/include).
HeaderFilterRegex: '^.*/(?:lib/(?!cli/include(?:/|$))|consensus|ledger|network|server|client|app)/.*'

FormatStyle: none
